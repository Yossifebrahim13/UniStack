import 'package:flutter/material.dart';

class MyQuestionsView extends StatelessWidget {
  const MyQuestionsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('My Questions')));
  }
}
